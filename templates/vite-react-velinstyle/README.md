# VelinStyle + React (Vite)

Minimal starter: VelinStyle CSS + component bundle, React 18, and `@birdapi/velinstyle-react` wrappers.

```bash
npm install
npm run dev
```

From the monorepo root, `velinstyle` resolves via `file:../..`. Publish or link the package for standalone use.
